syntax = "proto3";
package task;
option go_package = "github.com/AlexDigitalART/project-protos/proto/task";

// Сущность задача
message Task {
  uint32 id    = 1;
  string title = 2;
}

// Создание
message CreateTaskRequest  { string title = 1; }
message CreateTaskResponse { Task task = 1; }

// Получение одного
message GetTaskRequest  { uint32 id = 1; }

// Список всех
message ListTasksRequest  {}
message ListTasksResponse { repeated Task tasks = 1; }

// Обновление
message UpdateTaskRequest  { uint32 id = 1; string title = 2; }
message UpdateTaskResponse { Task task = 1; }

// Удаление
message DeleteTaskRequest  { uint32 id = 1; }
message DeleteTaskResponse { bool success = 1; }

service TaskService {
  rpc CreateTask (CreateTaskRequest)  returns (CreateTaskResponse);
  rpc GetTask    (Task)               returns (Task);
  rpc ListTasks  (ListTasksRequest)   returns (ListTasksResponse);
  rpc UpdateTask (UpdateTaskRequest)  returns (UpdateTaskResponse);
  rpc DeleteTask (DeleteTaskRequest)  returns (DeleteTaskResponse);
}
